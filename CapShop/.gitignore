# OS / editor
.DS_Store
Thumbs.db
.vs/
.vscode/
*.user
*.suo
*.userosscache
*.sln.docstates

# Logs / temp
*.log
*.tmp
*.temp
.capshop-pids

# Environment / secrets
.env
.env.local
.env.*.local
!.env.example
**/appsettings.Local.json
**/appsettings.*.local.json
**/appsettings.Secrets.json

# Node / frontend
node_modules/
dist/
coverage/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# .NET / build outputs
bin/
obj/
artifacts/
TestResults/

# Docker overrides / local-only files
# NOTE: docker-compose.yml is tracked in git — it is needed for VM deployment
docker-compose.override.yml

# ── Terraform ────────────────────────────────────────────────────────────────
# NEVER commit secrets or state!
terraform/terraform.tfvars
terraform/*.tfvars
terraform/capshop_vm.pem        # Generated SSH private key — keep safe!
terraform/.terraform/
terraform/.terraform.lock.hcl
terraform/terraform.tfstate
terraform/terraform.tfstate.backup
terraform/tfplan
terraform/*.tfplan
terraform/.terraform.tfstate.lock.info
