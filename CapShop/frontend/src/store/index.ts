/**
 * Central store export file.
 * Import stores from here — not directly from their files —
 * so if you ever restructure, you only change this file.
 *
 * Usage:
 *   import { useAuthStore, useCartStore } from "../store";
 */
export { useAuthStore } from "./authStore";
export { useCartStore } from "./cartStore";
